# =============================================================================
# Python Agent (FastAPI + LangGraph) —— 依赖清单
# -----------------------------------------------------------------------------
# 安装: pip install -r requirements.txt
# 启动: uvicorn main:app --host 0.0.0.0 --port 8000
# =============================================================================

# ---- FastAPI 核心 ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0

# ---- LangChain / LangGraph ----
# 版本固定：LangGraph 0.2.x + LangChain 0.3.x 接口最稳定
langgraph==0.2.60
langchain==0.3.14
langchain-core==0.3.29

# ---- HTTP 客户端（OpenAI 兼容接口 + Java agent-gateway） ----
httpx==0.27.2

# ---- Redis（会话记忆） ----
redis==5.2.1

# ---- 工具 ----
python-dotenv==1.0.1
loguru==0.7.2
