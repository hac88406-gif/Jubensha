<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <!-- ========================================================================== -->
    <!-- agent-gateway : Agent 中间层网关 (端口 8085, Servlet 栈)                     -->
    <!-- 技术栈: Spring Boot 3.2.5 | Spring Cloud 2023.x | Sentinel 1.8.6 (Servlet) -->
    <!--         OpenFeign 调用 Python Agent + Java 微服务                            -->
    <!-- ========================================================================== -->

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.urban.script</groupId>
        <artifactId>urban-script-reservation</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>agent-gateway</artifactId>
    <packaging>jar</packaging>
    <name>agent-gateway</name>
    <description>Agent 中间层网关 (端口 8085) —— 跨 Python Agent 与 Java 微服务的桥接层</description>

    <dependencies>
        <!-- ========== 公共模块 ========== -->
        <dependency>
            <groupId>com.urban.script</groupId>
            <artifactId>reservation-common</artifactId>
        </dependency>

        <!-- ========== Spring Boot Web (Servlet 栈！) ========== -->
        <!-- 注意：这里是 Servlet 栈 + Sentinel 1.8.6，不是 Gateway 的 WebFlux 栈！ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ========== Spring Cloud Nacos ========== -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>

        <!-- ========== OpenFeign + LoadBalancer ========== -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>

        <!-- ========== Sentinel ========== -->
        <!-- 已注释：Windows 环境下 Sentinel 1.8.6 存在 DateFileLogHandler NPE / ContextUtil 初始化失败问题 -->
        <!-- FallbackFactory 机制不依赖 Sentinel，Feign 异常时依然触发兜底 -->
        <!-- 如需重新启用 Linux/Mac 环境，取消注释下方两行：
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.csp</groupId>
            <artifactId>sentinel-datasource-nacos</artifactId>
        </dependency>
        -->

        <!-- ========== Knife4j ========== -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
            <version>4.5.0</version>
        </dependency>

        <!-- ========== 其他 ========== -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <!-- ========== 测试 ========== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- 可执行 jar（父 POM 默认 skip=true，这里显式开启） -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
